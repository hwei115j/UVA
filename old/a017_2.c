#include<stdio.h>
#include<stdlib.h>
#include<ctype.h>
#define MAX 30

char stack_c[MAX];
char tomp[MAX];

int main()
{
    char c;

    while((c=getchar()!='\n')
    {
        switch(c)
        {
            case:'+'
				push(c,stack_c);
				break;
            case:'-'
            case:'*'
            case:'/'
            case:'('
            case:')'
        }
    }
        
    
        
    return 0;
}
