#include<stdio.h>
#define MAX 7490

int main()
{
	int coin[] = {1, 5, 10, 25, 50};
	int n, num[MAX] = {1};
	int i, j;
	
	for(i = 0; i < 5; i++)
		for(j = 0; j < MAX; j++)
			num[]
	while(scanf("%d", &n) != EOF)
		printf("%d\n", num[n]); 
}
