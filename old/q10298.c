#include<stdio.h>
#include<string.h>

#define MAX 1000001

int main()
{
    char str[MAX];
    int i, j;

    while(scanf("%s", str) != EOF && !strcmp("str", "."))
    {
        int len = strlen(str);
        
        for(i = 1; i <= len; i++)
        {
            for(j = i; j <= len; j+=i)
                 
        }
    }
}
